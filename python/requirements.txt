# Core libraries
fastapi==0.100.0
uvicorn==0.22.0
pydantic==1.10.12  # Changed from 2.0.0 to be compatible with fastapi 0.100.0
# pydantic-settings==0.2.5  # Commented out to resolve dependency conflicts
pytest==7.4.3  # Updated from 7.3.1 to be compatible with pytest-env
pytest-asyncio==0.21.0  # Fixed version
PyJWT==2.8.0  # Added for JWT token handling

# Swiss Ephemeris library
pyswisseph==2.10.3.2
# swisseph>=0.6.0  # Removed - using pyswisseph exclusively
flatlib==0.2.0  # Astrology calculation library

# Utilities
python-multipart==0.0.19
httpx==0.26.0
psutil==5.9.5
python-jose==3.4.0
passlib==1.7.4
bcrypt==4.0.1
redis==5.0.1  # Updated to match redis version below (removed duplicate)
python-dotenv==1.0.0  # Moved from other section for better grouping
jsonschema==4.20.0  # Added for websocket validation
googlemaps==4.10.0  # Updated from 5.0.0 to 4.10.0 (highest available version)
geopy==2.4.0
timezonefinder==5.2.0
# Data handling
pytz==2023.3
numpy==1.26.4
pandas==2.2.1
requests==2.32.2
urllib3==1.26.18  # Downgraded to be compatible with LibreSSL 2.8.3

# Connection and networking
aiohttp==3.9.0  # Changed from beta version
starlette==0.27.0  # Updated to be compatible with FastAPI 0.100.0
itsdangerous==2.1.2
jinja2==3.1.6
aiofiles==23.1.0
websockets==12.0

# AI and ML dependencies
scikit-learn==1.5.1
tensorflow==2.15.0  # Using base package for container compatibility
torch==2.6.0  # Updated to latest installed version
filelock==3.18.0  # Required by PyTorch
fsspec==2023.9.0  # Downgraded from future version
networkx==3.4.2  # Required by PyTorch
sympy==1.13.1  # Required by PyTorch
mpmath==1.3.0  # Required by sympy/PyTorch
transformers==4.48.0  # Added for unified rectification model
openai==1.12.0  # For OpenAI API access
tenacity==8.2.0  # For retry logic

# Testing and development
pytest-cov==4.1.0
black==24.3.0
isort==5.13.2
mypy==1.8.0
ruff==0.1.11
pytest-xdist==3.5.0
pytest-mock==3.12.0
pytest-env==1.1.3
pytest-timeout==2.2.0
selenium==4.18.1  # Added for UI testing
webdriver-manager==4.0.1  # Added for managing webdriver
# playwright==1.40.0  # Added for testing with Playwright

# Documentation
sphinx==7.2.6
sphinx-rtd-theme==2.0.0
mkdocs==1.5.3
mkdocs-material==9.5.11

# Monitoring and logging
prometheus-client==0.20.0
python-json-logger==2.0.7
structlog==24.1.0

# Email validator
email-validator==2.1.0  # Required for Pydantic email validation
websocket-client==1.7.0  # Added for WebSocket client connections

# Data visualization and chart generation
matplotlib==3.8.3  # For chart rendering
Pillow==10.1.0  # PIL fork for image processing
# mpl_toolkits.mplot3d is included in matplotlib, not a separate package

# PDF Generation
reportlab==4.1.0  # For PDF generation
weasyprint==60.2  # HTML to PDF conversion alternative

# Redis for session storage and caching
hiredis==2.3.2  # C accelerator for redis-py

# Database
asyncpg==0.29.0  # PostgreSQL database driver for asyncio
alembic==1.13.1  # Database migrations

# Astronomy/Astrological calculations
ephem==4.1.5  # Astronomical calculations
jplephem==2.20  # JPL Ephemerides for precise planetary calculations
skyfield==1.46  # High-precision astronomy
astropy==6.0.0  # Comprehensive astronomy library
# swisseph==0.6.0  # Swiss Ephemeris for astrological calculations (used in swisseph_direct) - REMOVED - using pyswisseph instead

# Update problem dependencies
PyYAML==6.0.1  # Upgraded from 5.x to fix build error

# WebSocket service dependencies
uuid==1.30  # For generating unique identifiers

# traceback2==1.4.0  # For enhanced traceback handling
traceback2==1.4.0
